{
  "name": "salesorder-app",
  "version": "1.0.0",
  "description": "Express + vanilla-JS frontend that creates and lists NetSuite Sales Orders via RESTlet (TBA OAuth 1.0a).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-rate-limit": "^7.4.1",
    "morgan": "^1.10.0"
  },
  "engines": {
    "node": ">=18"
  }
}
